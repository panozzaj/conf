#!/bin/sh

[ -S .zeus.sock ] && z="zeus"

if [ -n "$z" ]; then
  echo "Running under zeus."
else
  echo "No Zeus enviroment detected."
fi

if [ $1 ]; then
  $z rake routes | grep $1
else
  $z rake routes
fi
